'use client';
import { useState } from 'react';
import Header from '@/components/Header';
import { ArrowRight } from 'lucide-react';
export default function NewRoom(){const[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');try{const values=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/rooms',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(values)});const data=await r.json();if(r.status===401){window.location.href='/login?next=/rooms/new';return;}if(!r.ok)throw new Error(data.error);window.location.href=`/rooms/${data.id}`;}catch(e){setError((e as Error).message);setBusy(false);}}
return <><Header/><main className="form-wrap"><span className="eyebrow">A fresh page / 01</span><h1 style={{marginTop:15}}>Make some study space.</h1><p className="muted">Give your room a name. Bring the notes once you’re in, then invite your people.</p><form onSubmit={submit}><label>Room name<input name="name" placeholder="e.g. The Friday biology table" maxLength={100} minLength={2} required/></label><label>Your name at the table<input name="displayName" placeholder="What should we call you?" maxLength={50} required/></label><label>What are we focusing on? <span className="muted">Optional</span><textarea name="studyFocus" maxLength={2000} placeholder="Focus on chapter 3. Skip the historical background. Explain things with examples."/></label>{error&&<p className="error" role="alert">{error}</p>}<button disabled={busy}>{busy?'Preparing your room…':'Open the room'}<ArrowRight size={16}/></button><small>Only people with your room link can join after signing in.</small></form></main></>;}
